<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewmodel"
            type="gyul.songgyubin.daytogo.viewmodel.AuthViewModel" />

        <variable
            name="activity"
            type="gyul.songgyubin.daytogo.view.SignInActivity" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:id="@+id/ed_email"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="50dp"
            android:layout_marginRight="50dp"
            android:autofillHints="emailAddress"
            android:hint="@string/enter_email"
            android:onTextChanged="@{viewmodel::onEmailTextChanged}"
            android:text="@={viewmodel.inputEmail}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_warning_email"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/enter_valid_email"
            android:textColor="#ff0000"
            android:visibility="@{viewmodel.isValidEmail() ? View.GONE : View.VISIBLE}"
            app:layout_constraintLeft_toLeftOf="@+id/ed_email"
            app:layout_constraintTop_toBottomOf="@id/ed_email" />

        <EditText
            android:id="@+id/ed_password"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="50dp"
            android:layout_marginRight="50dp"
            android:autofillHints="password"
            android:hint="@string/enter_password"
            android:inputType="textPassword"
            android:text="@={viewmodel.inputPassword}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_warning_email" />



        <Button
            android:id="@+id/btn_firebase_login"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:onClick="@{viewmodel::firebaseLoginSingleClickEvent}"
            android:text="@string/start_with_email"
            android:textStyle="bold"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginLeft="50dp"
            android:layout_marginRight="50dp"
            app:layout_constraintTop_toTopOf="@+id/ed_password"
            app:layout_constraintBottom_toBottomOf="parent"
             />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:onClick="@{activity::startSignUp}"
            android:text="@string/signup_with_email"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/btn_firebase_login" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>